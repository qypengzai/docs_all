---
docType: API
date: "2026-08-04"
---

# ZegoCustomAudioSource

## ZegoCustomAudioSource

*Declared in `ZegoCustomAudioSource.java`*

### 方法
<ParamField
  name="getIndex"
  prototype="public  int getIndex()"
  desc="获取自定义音频源实例索引。"
  parent_file="Declared in \`ZegoCustomAudioSource.java\`"
  parent_name="ZegoCustomAudioSource"
  parent_type="class">
自定义音频源实例索引。
</ParamField>
<ParamField
  name="startCapture"
  prototype="public  int startCapture()"
  desc="启动自定义音频源采集。"
  parent_file="Declared in \`ZegoCustomAudioSource.java\`"
  parent_name="ZegoCustomAudioSource"
  parent_type="class">
启动自定义音频源采集。

<Note title="">
- **业务场景**：通常用于需要在不同的音频采集源之间切换的场景下。
- **调用时机**：在创建自定义音频源 [createCustomAudioSource] 后调用生效。
</Note>

<Warning title="">
- **支持版本**：3.25.0 及以上。
- **使用限制**：无。
- **注意事项**：ZegoCustomAudioSource 仅支持作为混音源使用，不支持作为主采集源使用。
</Warning>

接口调用结果。
</ParamField>
<ParamField
  name="stopCapture"
  prototype="public  int stopCapture()"
  desc="停止自定义音频源采集。"
  parent_file="Declared in \`ZegoCustomAudioSource.java\`"
  parent_name="ZegoCustomAudioSource"
  parent_type="class">
停止自定义音频源采集。

<Note title="">
- **业务场景**：通常用于需要在不同的音频采集源之间切换的场景下。
- **调用时机**：在创建自定义音频源 [createCustomAudioSource] 后调用生效。
</Note>

<Warning title="">
- **支持版本**：3.25.0 及以上。
- **使用限制**：无。
- **注意事项**：ZegoCustomAudioSource 仅支持作为混音源使用，不支持作为主采集源使用。
</Warning>

接口调用结果。
</ParamField>
<ParamField
  name="enableAEC"
  prototype="public  int enableAEC(boolean enable)"
  desc="启用自定义音频源回声消除。"
  parent_file="Declared in \`ZegoCustomAudioSource.java\`"
  parent_name="ZegoCustomAudioSource"
  parent_type="class">
**参数**

| 名称 | 类型 | 描述 |
| --- | --- | --- |
| enable | boolean | 是否启用回声消除。 |

**详情**

启用自定义音频源回声消除。

<Note title="">
- **业务场景**：通常用于需要在不同的音频采集源之间切换的场景下。
- **调用时机**：在创建自定义音频源 [createCustomAudioSource] 后调用生效。
</Note>

<Warning title="">
- **支持版本**：3.25.0 及以上。
- **使用限制**：无。
- **注意事项**：ZegoCustomAudioSource 仅支持作为混音源使用，不支持作为主采集源使用。
</Warning>

**返回值**

接口调用结果。
</ParamField>
<ParamField
  name="setVolume"
  prototype="public  int setVolume(int volume)"
  desc="设置自定义音频源音量。"
  parent_file="Declared in \`ZegoCustomAudioSource.java\`"
  parent_name="ZegoCustomAudioSource"
  parent_type="class">
**参数**

| 名称 | 类型 | 描述 |
| --- | --- | --- |
| volume | int | 音量大小，范围为 0 ~ 100。 |

**详情**

设置自定义音频源的音量大小。

<Note title="">
- **业务场景**：通常用于需要在不同的音频采集源之间切换的场景下。
- **调用时机**：在创建自定义音频源 [createCustomAudioSource] 后调用生效。
</Note>

<Warning title="">
- **支持版本**：3.25.0 及以上。
- **使用限制**：音量范围为 0 ~ 100。
- **注意事项**：ZegoCustomAudioSource 仅支持作为混音源使用，不支持作为主采集源使用。
</Warning>

**返回值**

接口调用结果。
</ParamField>
<ParamField
  name="mute"
  prototype="public  int mute(boolean mute)"
  desc="静音或取消静音自定义音频源。"
  parent_file="Declared in \`ZegoCustomAudioSource.java\`"
  parent_name="ZegoCustomAudioSource"
  parent_type="class">
**参数**

| 名称 | 类型 | 描述 |
| --- | --- | --- |
| mute | boolean | 是否静音自定义音频源。 |

**详情**

静音或取消静音自定义音频源的音频数据。

<Note title="">
- **业务场景**：通常用于需要在不同的音频采集源之间切换的场景下。
- **调用时机**：在创建自定义音频源 [createCustomAudioSource] 后调用生效。
</Note>

<Warning title="">
- **支持版本**：3.25.0 及以上。
- **使用限制**：无。
- **注意事项**：ZegoCustomAudioSource 仅支持作为混音源使用，不支持作为主采集源使用。
</Warning>

**返回值**

接口调用结果。
</ParamField>
<ParamField
  name="pushAudioFrame"
  prototype="public  int pushAudioFrame(ByteBuffer data, int dataLength, [ZegoAudioFrameParam](../../api-reference/class#zegoaudioframeparam) param)"
  desc="推送音频帧到自定义音频源。"
  parent_file="Declared in \`ZegoCustomAudioSource.java\`"
  parent_name="ZegoCustomAudioSource"
  parent_type="class">
**参数**

| 名称 | 类型 | 描述 |
| --- | --- | --- |
| data | ByteBuffer | PCM buffer 数据。注意必须是 DirectByteBuffer 类型，可以通过 allocateDirect() 构造。 |
| dataLength | int | buffer 数据的总长度。 |
| param | [ZegoAudioFrameParam](../../api-reference/class#zegoaudioframeparam) | 该 PCM 音频帧的参数。 |

**详情**

推送音频帧到自定义音频源。

<Note title="">
- **业务场景**：通常用于开发者自己管理音频数据采集，并将音频帧输入到指定自定义音频源的场景。
- **调用时机**：在创建自定义音频源 [createCustomAudioSource] 后调用生效。
</Note>

<Warning title="">
- **支持版本**：3.25.0 及以上。
- **使用限制**：无。
- **注意事项**：ZegoCustomAudioSource 仅支持作为混音源使用，不支持作为主采集源使用。
</Warning>

**返回值**

接口调用结果。
</ParamField>
