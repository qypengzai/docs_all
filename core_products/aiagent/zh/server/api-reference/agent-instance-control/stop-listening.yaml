openapi: 3.0.0
info:
  title: open-api-desc
  version: 1.0.0
  contact:
    name: AI Agent API Support
    email: support@zegocloud.com

servers:
  $ref: '../shared-components.yaml#/servers'

tags:
  - name: agent-instance-control

paths:
  /:
    post:
      tags:
        - agent-instance-control
      summary: StopListening
      description: 对讲机模式下，智能体结束聆听。对讲机模式开启请参考[创建智能体实例](/aiagent-server/api-reference/agent-instance-management/create-agent-instance)的 AdvancedConfig.CommunicationMode 参数说明。

        > 该能力也可以通过客户端 **ZEGO Express SDK** 发送房间信令实现相同效果，无需服务端接口调用。详情请查看客户端文档[智能体自定义控制](/aiagent-android/guides/agent-custom-control)。
      operationId: stop-listening
      parameters:
        - name: Action
          in: query
          description: |
            > 接口原型参数
            >
            > https://aigc-aiagent-api.zegotech.cn?Action=StopListening
          required: true
          allowEmptyValue: false
          schema:
            type: string
            enum: [StopListening]
          style: form
          explode: true
        # 引用共享的参数
        - $ref: '../shared-components.yaml#/components/parameters/AppId'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureNonce'
        - $ref: '../shared-components.yaml#/components/parameters/Timestamp'
        - $ref: '../shared-components.yaml#/components/parameters/Signature'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureVersion'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StopListeningRequest"
            example:
              AgentInstanceId: "1907755175297171456"
              Sequence: 2
      responses:
        "200":
          description: 结束聆听成功
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StopListeningResponse"

components:
  schemas:
    StopListeningRequest:
      type: object
      required:
        - AgentInstanceId
      properties:
        AgentInstanceId:
          type: string
          description: Agent 实例的唯一标识，全局唯一。
          example: "1907755175297171456"
        UserId:
          type: string
          description: 结束聆听模式的用户。默认使用创建的时候绑定的UserId。
          example: "user_123"
        Sequence:
          type: integer
          format: int64
          description: |
            客户端自己定义的跟随请求有序增加的序列号。每个用户/设备独立递增。AI Agent 后台只处理 Sequence 最新的请求。如果不传，则按 AI Agent 后台接收顺序处理，由于网络问题可能会导致用户说话的顺序和后台接收的顺序不一致。

            > **📌 重要说明**
            >
            > StartListening 和 StopListening 接口的 Sequence 必须有序。比如：
            > 1. StartListening(Sequence:1)
            > 2. StopListening(Sequence:2)
            > 3. StartListening(Sequence:3)
            > 4. StopListening(Sequence:4)
            > 5. ...
          example: 2
        CancelFlag:
          type: boolean
          description: |
            是否取消本轮聆听。默认 false。
            - false（默认）：正常结束聆听，进入 ASR/LLM 流程，AI 将根据用户说话内容进行回复。
            - true：取消本轮聆听。停止 ASR，丢弃本轮缓存的音频和已识别结果，不外抛 ASR 终态回调、不请求 LLM/TTS、不写入历史。Agent 实例状态将从 listening 变为 idle。
          default: false
          example: false
    StopListeningResponse:
      type: object
      properties:
        Code:
          type: integer
          description: 返回码，0 表示成功，其他值表示失败。详情请参考 [返回码](/aiagent-server/api-reference/return-codes) 说明。
          example: 0
        Message:
          type: string
          description: 请求结果说明
          example: "success"
        RequestId:
          type: string
          description: 请求 ID
          example: "8916619579948490296"
        Data:
          type: object
          description: 响应数据
          properties:
            Round:
              type: integer
              format: int64
              description: 本轮的 Round（轮次）
              example: 158422001

