openapi: 3.0.0
info:
  title: open-api-desc
  version: 1.0.0
  contact:
    name: AI Agent API Support
    email: support@zegocloud.com

servers:
  - url: https://aigc-aiagent-api.zegotech.cn
    description: 统一接入地址（不区分区域）
  - url: https://aigc-aiagent-api-sha.zegotech.cn
    description: 中国大陆（上海）

tags:
  - name: agent-instance-management

paths:
  /:
    post:
      tags:
        - agent-instance-management
      summary: UpdateAgentInstance
      description: |
        通过本接口，您可以更新智能体实例的配置。

        <Warning title="警告">
          - 可以分别单独更新 LLM、TTS、ASR、RTC 的配置，也可以同时更新多个配置。
          - LLM、TTS、ASR 三个配置项是覆盖式更新，即传入的配置会覆盖原有的配置（比如只要更新 LLM.Model 也需要保证 LLM 的其他参数设置了正确值）。
        </Warning>
      operationId: update-agent-instance
      parameters:
        - name: Action
          in: query
          description: |
            > 接口原型参数
            >
            > https://aigc-aiagent-api.zegotech.cn?Action=UpdateAgentInstance
          required: true
          allowEmptyValue: false
          schema:
            type: string
            enum: [UpdateAgentInstance]
          style: form
          explode: true
        # 引用共享的参数
        - $ref: '../shared-components.yaml#/components/parameters/AppId'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureNonce'
        - $ref: '../shared-components.yaml#/components/parameters/Timestamp'
        - $ref: '../shared-components.yaml#/components/parameters/Signature'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureVersion'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAgentInstanceRequest"
            example:
              AgentInstanceId: "1907755175297171456"
              LLM:
                Url: "https://ark.cn-beijing.volces.com/api/v3/chat/completions"
                ApiKey: "zego_test"
                Model: "doubao-1-5-lite-32k-250115"
                SystemPrompt: "你是小智，成年女性，是**即构科技创造的陪伴助手**，上知天文下知地理，聪明睿智、热情友善。"
      responses:
        "200":
          description: 更新成功
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateAgentInstanceResponse"

components:
  schemas:
    UpdateAgentInstanceRequest:
      type: object
      required:
        - AgentInstanceId
      properties:
        AgentInstanceId:
          type: string
          description: 智能体实例的唯一标识，通过 [创建智能体实例](/aiagent-server/api-reference/agent-instance-management/create-agent-instance) 接口的响应参数获取。
          example: "1907755175297171456"
        LLM:
          $ref: '../shared-components.yaml#/components/schemas/LLM'
        TTS:
          $ref: '../shared-components.yaml#/components/schemas/TTS'
        ASR:
          $ref: '../shared-components.yaml#/components/schemas/ASR'
        RTC:
          type: object
          description: RTC 参数信息，用于更新用户推流使用的流 ID。
          properties:
            UserStreamId:
              type: string
              description: 真实用户推流使用的流 ID。
              maxLength: 128
              example: "user_stream_1"
        AdvancedConfig:
          type: object
          description: |
            高级配置。当前仅支持更新 `CommunicationMode` 参数。
          properties:
            CommunicationMode:
              type: integer
              description: |
                互动模式，仅支持全双工模式与聆听模式之间的切换，从下一句用户说话开始生效：
                - 0：全双工模式。用户随时可以说话。
                - 1：聆听模式。智能体仅持续聆听用户说话，不主动请求 LLM 回复。

                > **📌 重要说明**
                >
                > 不支持切换到对讲机模式（2）；对讲机模式仅在[创建智能体实例](/aiagent-server/api-reference/agent-instance-management/create-agent-instance)时设置。
              enum: [0, 1]
              example: 1

    UpdateAgentInstanceResponse:
      type: object
      properties:
        Code:
          type: integer
          description: 返回码，0 表示成功，其他值表示失败。详情请参考 [返回码](/aiagent-server/api-reference/return-codes) 说明。
          example: 0
        Message:
          type: string
          description: 请求结果说明
          example: "success"
        RequestId:
          type: string
          description: 请求 ID
          example: "3151527792559699732"
