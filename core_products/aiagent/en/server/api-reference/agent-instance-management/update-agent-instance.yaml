openapi: 3.0.0
info:
  title: open-api-desc
  version: 1.0.0
  contact:
    name: AI Agent API Support
    email: support@zegocloud.com

servers:
  - url: https://aigc-aiagent-api.zegotech.cn
    description: Unified access address (no regional distinction)
  - url: https://aigc-aiagent-api-sha.zegotech.cn
    description: Mainland China (Shanghai)
  - url: https://aigc-aiagent-api-hkg.zegotech.cn
    description: Hong Kong, Macao, and Taiwan (Hong Kong)
  - url: https://aigc-aiagent-api-fra.zegotech.cn
    description: Europe (Frankfurt)
  - url: https://aigc-aiagent-api-lax.zegotech.cn
    description: Western United States (California)
  - url: https://aigc-aiagent-api-bom.zegotech.cn
    description: Asia-Pacific (Mumbai)
  - url: https://aigc-aiagent-api-sgp.zegotech.cn
    description: Southeast Asia (Singapore)

tags:
  - name: agent-instance-management

paths:
  /:
    post:
      tags:
        - agent-instance-management
      summary: UpdateAgentInstance
      description: |
        With this interface, you can update the configuration of the AI agent instance.

        <Warning title="Warning">
          - You can update the configuration of LLM, TTS, ASR, and RTC separately, or update multiple configurations at the same time.
          - The LLM, TTS, and ASR configuration items are updated in a cover-up manner, meaning that the incoming configuration will overwrite the existing configuration (for example, if you only update LLM.Model, you need to ensure that the other parameters of LLM are set correctly).
        </Warning>
      operationId: update-agent-instance
      parameters:
        - name: Action
          in: query
          description: |
            > API Prototype Parameter
            >
            > https://aigc-aiagent-api.zegotech.cn?Action=UpdateAgentInstance
          required: true
          allowEmptyValue: false
          schema:
            type: string
            enum: [UpdateAgentInstance]
          style: form
          explode: true
        - $ref: '../shared-components.yaml#/components/parameters/AppId'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureNonce'
        - $ref: '../shared-components.yaml#/components/parameters/Timestamp'
        - $ref: '../shared-components.yaml#/components/parameters/Signature'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureVersion'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAgentInstanceRequest"
            example:
              AgentInstanceId: "1907755175297171456"
              LLM:
                Url: "https://ark.cn-beijing.volces.com/api/v3/chat/completions"
                ApiKey: "zego_test"
                Model: "doubao-1-5-lite-32k-250115"
                SystemPrompt: "You are Xiao Zhi, an adult female and **the companion assistant created by ZEGO**. You are knowledgeable, smart, wise, warm, and friendly."
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateAgentInstanceResponse"

components:
  schemas:
    UpdateAgentInstanceRequest:
      type: object
      required:
        - AgentInstanceId
      properties:
        AgentInstanceId:
          type: string
          description: The unique identifier of the AI agent instance, obtained through the response parameters of the [Create AI Agent Instance](/aiagent-server/api-reference/agent-instance-management/create-agent-instance) interface.
          example: "1907755175297171456"
        LLM:
          $ref: '../shared-components.yaml#/components/schemas/LLM'
        TTS:
          $ref: '../shared-components.yaml#/components/schemas/TTS'
        ASR:
          $ref: '../shared-components.yaml#/components/schemas/ASR'
        RTC:
          type: object
          description: RTC parameter information, used to update the user's stream ID.
          properties:
            UserStreamId:
              type: string
              description: The stream ID used by the real user for streaming.
              maxLength: 128
              example: "user_stream_1"
        AdvancedConfig:
          type: object
          description: |
            Advanced configuration. Currently only the `CommunicationMode` parameter can be updated.
          properties:
            CommunicationMode:
              type: integer
              description: |
                Interaction mode. Only switching between full-duplex mode and listening mode is supported, and it takes effect from the user's next utterance:
                - 0: Full-duplex mode. The user can speak at any time.
                - 1: Listening mode. The agent only continuously listens to the user without proactively requesting LLM responses.

                > **📌 Important Note**
                >
                > Switching to walkie-talkie mode (2) is not supported; walkie-talkie mode can only be set when [creating an agent instance](/aiagent-server/api-reference/agent-instance-management/create-agent-instance).
              enum: [0, 1]
              example: 1

    UpdateAgentInstanceResponse:
      type: object
      properties:
        Code:
          type: integer
          description: Return code. 0 indicates success, other values indicate failure. For more information on error codes and response handling recommendations, please refer to [Return Codes](/aiagent-server/api-reference/return-codes).
          example: 0
        Message:
          type: string
          description: Explanation of the request result
          example: "Success"
        RequestId:
          type: string
          description: Request ID
          example: "8825223157230377926"
