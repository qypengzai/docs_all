---
articleID: 21505
date: "2025-12-26"
---
# 原始音频数据获取

- - -

## 功能简介

SDK 为开发者提供了获取原始音频数据的功能，获取的原始音频数据格式为 PCM，开发者可以将此数据写到本地设备中，实现录制音频。

## 前提条件

在获取原始音频数据之前，请确保：

- 已在 [ZEGO 控制台](https://console.zego.im) 创建项目，并申请有效的 AppID 和 AppSign，详情请参考 [控制台 - 项目信息](/console/project-info)。
- 已在项目中集成 ZEGO Express SDK，并实现了基本的音视频推拉流功能，详情请参考 [快速开始 - 集成](../quick-start/integrating-sdk.mdx) 和 [快速开始 - 实现流程](../quick-start/implementing-video-call.mdx)。



## 使用步骤

### 1 初始化 SDK

:::if{props.Product=undefined}
请参考 [快速开始 - 实现流程](../quick-start/implementing-video-call.mdx) 的 “创建引擎”。
:::
:::if{props.Product="vioce"}
请参考 [快速开始 - 实现流程](../quick-start/implementing-vioce-call.mdx) 的 “创建引擎”。
:::

:::if{props.Product="live-streaming"}
请参考 [快速实现超低延迟直播 - 实现流程](../quick-start/implementing-live-streaming.mdx#初始化) 的 “初始化”。
:::

### 2 开启获取原始音频数据功能

开发者可调用 [startAudioDataObserver](@startAudioDataObserver) 接口，开启原始音频数据回调监测。回调的音频数据类型为 [ZegoAudioDataCallbackBitMask](@-ZegoAudioDataCallbackBitMask)，包括有 `CAPTURED`、`PLAYBACK`、`MIXED`、`PLAYER`，这四个回调类型都需要开启。

```cs
ZegoAudioFrameParam param = new ZegoAudioFrameParam();
param.channel = ZegoAudioChannel.Stereo;
param.sampleRate = ZegoAudioSampleRate.ZegoAudioSampleRate16K;pap
uint bitmask = 0;
// 添加 bitmask，开启采集到音频数据到回调开关。
// 采集、播放、混合、拉流对应的位掩码值分别是：CAPTURED=1，PLAYBACK=2，MIXED=4，PLAYER=8，bitmask 最终得到的值为 15，表示会同时触发采集、播放、混合、拉流的原始数据回调。
bitmask |= (uint)ZegoAudioDataCallbackBitMask.Captured;
bitmask |= (uint)ZegoAudioDataCallbackBitMask.Playback;
bitmask |= (uint)ZegoAudioDataCallbackBitMask.Mixed;
bitmask |= (uint)ZegoAudioDataCallbackBitMask.Player;
engine.StartAudioDataObserver(bitmask, param);

```
import Content from '/core_products/real-time-voice-video/zh/snippets/using-bitmask.mdx'

<Content />

### 3 设置回调接收原始音频数据并处理

开发者根据需要可实现回调 [onCapturedAudioData](@onCapturedAudioData)、[onPlaybackAudioData](@onPlaybackAudioData)、[onMixedAudioData](@onMixedAudioData)、[onPlayerAudioData](@onPlayerAudioData)，分别对应上述 [ZegoAudioDataCallbackBitMask](@-ZegoAudioDataCallbackBitMask) 中的四个音频数据类型。

```cs
void OnCapturedAudioData(IntPtr data, uint dataLength, ZegoAudioFrameParam param)
{
    // 本地采集音频数据，推流后可收到回调
}

void OnPlaybackAudioData(IntPtr data, uint dataLength, ZegoAudioFrameParam param)
{
    // SDK 播放的音频数据，在非拉流状态的引擎启动状态且未使用媒体播放器播放媒体文件状态时，回调的音频数据是静音的音频数据
}

void OnMixedAudioData(IntPtr data, uint dataLength, ZegoAudioFrameParam param)
{
    // 本地采集与 SDK 播放的声音混合后的音频数据回调
}

void OnPlayerAudioData(IntPtr data, uint dataLength, ZegoAudioFrameParam param, string streamID)
{
    // 远端拉流音频数据，开始拉流后每条拉流数据的回调
}

```

### 4 停止音频数据回调监测



若想停止音频数据回调监测，可调用 [stopAudioDataObserver ](@stopAudioDataObserver) 接口。

```cs
engine.StopAudioDataObserver();
```
