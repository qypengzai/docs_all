openapi: 3.0.0
info:
  title: open-api-desc
  version: 1.0.0

servers:
  - url: https://cloud-realtime-asr-api.zegotech.cn

paths:
  /:
    post:
      tags:
        - cloud-realtime-asr
      summary: StartRealtimeASRTask
      description: 开启云端实时语音识别任务，并把语音识别实例加入 RTC 对话中。
      operationId: start
      parameters:
        - name: Action
          in: query
          description: |
            > 接口原型参数
            >
            > https://cloud-realtime-asr-api.zegotech.cn?Action=StartRealtimeASRTask
          required: true
          allowEmptyValue: false
          schema:
            type: string
            enum: [StartRealtimeASRTask]
          style: form
          explode: true
        # 引用共享的参数
        - $ref: './shared-components.yaml#/components/parameters/AppId'
        - $ref: './shared-components.yaml#/components/parameters/SignatureNonce'
        - $ref: './shared-components.yaml#/components/parameters/Timestamp'
        - $ref: './shared-components.yaml#/components/parameters/SignatureVersion'
        - $ref: './shared-components.yaml#/components/parameters/Signature'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartASRTaskRequest"
            examples:
              "1 V 1 通话示例":
                summary: "1 V 1 通话示例"
                value:
                  RoomId: "string"
                  RecognitionRange: 0
                  ASR:
                    Vendor: "Tencent"
                    Params:
                      engine_model_type: "16k_zh"
                      hotword_list: "zego|10"
                    VADSilenceSegmentation: 500
                  AdvancedConfig:
                    MaxIdleTime: 120
              "多人通话翻译示例":
                summary: "多人通话翻译示例"
                value:
                  RoomId: "your_room_id"
                  RecognitionRange: 1
                  SubtitleType: 2
                  StreamList:
                    - StreamId: "s1"
                      ASR:
                        Vendor: "Tencent"
                        Params:
                          EngineModelType: "16k_zh"
                      EnableTranslation: true
                      Translation:
                        Vendor: "DoubaoSeedTranslation"
                        SourceLanguage: "zh"
                        TargetLanguage: "en"
                        LLM:
                          Url: "https://ark.cn-beijing.volces.com/api/v3/responses"
                          ApiKey: "your_doubao_api_key"
                          Model: "doubao-seed-translation-250915"
                    - StreamId: "s2"
                      ASR:
                        Vendor: "Tencent"
                        Params:
                          EngineModelType: "16k_en"
                      EnableTranslation: true
                      Translation:
                        Vendor: "DoubaoSeedTranslation"
                        SourceLanguage: "en"
                        TargetLanguage: "zh"
                        LLM:
                          Url: "https://ark.cn-beijing.volces.com/api/v3/responses"
                          ApiKey: "your_doubao_api_key"
                          Model: "doubao-seed-translation-250915"
      responses:
        "200":
          description: 开始任务成功
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartASRTaskResponse"

components:
  schemas:
    StartASRTaskRequest:
      type: object
      required:
        - RoomId
      properties:
        RoomId:
          type: string
          description: RTC 房间 Id
        RecognitionRange:
          type: integer
          description: 识别范围。0：房间内所有流，1：指定 StreamList。
          enum: [0, 1]
          example: 0
        ASR:
          $ref: "./shared-components.yaml#/components/schemas/ASRConfig"
        StreamList:
          type: array
          description: 需要识别的流列表，RecognitionRange 参数为 1 时生效。
          items:
            $ref: "./shared-components.yaml#/components/schemas/StreamListItem"
          example: []
        AdvancedConfig:
          type: object
          description: 高级配置参数。
          properties:
            MaxIdleTime:
              type: integer
              description: RTC 房间内超过 MaxIdleTime 没有人推流，后台自动停止识别任务，单位为秒。
              minimum: 30
              maximum: 1800
              default: 120
              example: 120
        EnableTranslation:
          type: boolean
          description: 是否开启翻译。默认不开启。
        Translation:
          type: object
          description: 翻译 LLM 配置项
          required:
            - Vendor
            - SourceLanguage
            - TargetLanguage
            - LLM
          properties:
            Vendor:
              type: string
              description: 调用 LLM 供应商。
              enum: [DoubaoSeedTranslation, QwenMT]
              example: DoubaoSeedTranslation
            SourceLanguage:
              type: string
              description: 源语言
              example: zh
            TargetLanguage:
              type: string
              description: 目标语言
              example: en
            LLM:
              type: object
              description: 用于翻译的 LLM 参数。
              required:
                - Url
                - ApiKey
                - Model
              properties:
                Url:
                  type: string
                  description: |
                    调用 LLM 接口地址。不同 Vendor 不同：
                    - Vendor 为 DoubaoSeedTranslation 时：https://ark.cn-beijing.volces.com/api/v3/responses
                    - Vendor 为 QwenMT 时：https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
                  example: "https://ark.cn-beijing.volces.com/api/v3/responses"
                ApiKey:
                  type: string
                  description: LLM 服务提供商用于认证的参数。
                  example: ""
                Model:
                  type: string
                  description: 所调用的 LLM 模型名称，必填。不同供应商支持的模型不同。
                  example: "doubao-seed-translation-250915"
                Params:
                  type: object
                  description: |-
                    LLM 或应用平台的请求参数，选填。<br>
                    - 对于 OpenAI 协议，可配置如 max_tokens, temperature, stream 等参数。<br>
                    - 对于其他应用平台，可填写平台支持的参数。
                  example: {}
        SubtitleType:
          type: integer
          description: |
            字幕下发类型，默认值为 0。<br>
            0: 不下发；<br>
            1: 仅下发识别结果（如用户说中文"你好"，则下发"你好"）；<br>
            2: 仅下发翻译结果（如用户说中文"你好"，则下发翻译后的"Hello"）；<br>
            3: 同时下发识别结果和翻译结果（如用户说中文"你好"，则同时下发"你好"和"Hello"）。<br>
            若需要客户端展示UI，则推荐配置 1、2 或 3。详情见[展示字幕](/cloud-realtime-asr/guides/display-subtitles)。
        CallbackConfig:
          type: object
          description: 回调配置。
          properties:
            HostTag:
              type: string
              description: |
                可以设置最多两个回调地址用于监听任务过程中所发生的事件。通过该参数指定该任务的回调发送到哪个地址。具体 tag 值需联系 ZEGO 技术支持预先配置好，当前支持 Custom1、Custom2。
              example: "Custom1"

    StartASRTaskResponse:
      type: object
      properties:
        Code:
          type: integer
          description: 返回码，0 表示成功，其他值表示失败。详情请参考 [返回码](/cloud-realtime-asr/api-reference/return-codes) 说明。
          example: 0
        Message:
          type: string
          description: 请求结果说明
          example: "Success"
        RequestId:
          type: string
          description: 请求 ID
          example: "3151527792559699732"
        Data:
          type: object
          properties:
            TaskId:
              type: string
              description: 任务的唯一标识。
              example: "1920370518175780864"
